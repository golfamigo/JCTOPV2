{
  "name": "jctop-frontend",
  "installCommand": "npm install --legacy-peer-deps || true",
  "buildCommand": "echo 'No build needed for Expo dev server'",
  "startCommand": "npx expo start --web --port 3000 --host 0.0.0.0",
  "environment": {
    "NODE_VERSION": "18",
    "EXPO_PUBLIC_API_URL": "${BACKEND_URL}/api/v1"
  }
}